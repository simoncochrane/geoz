package graph

/*
// Geometry is an interface defining methods required for graph calculations.
type Geometry interface {
	Empty() bool
	Dimension() int
	BoundaryDimension() int
	IsRings() bool

	Envelope() *coord.Envelope

	Graph(index int) (*Graph, error)
}
*/
